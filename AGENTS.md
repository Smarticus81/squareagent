# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## Project Overview

VoyceLab -- a voice-powered POS operations platform for hospitality venues (bars, restaurants, event spaces). Users create voice assistants that connect to Square POS. Natural voice conversations turn into real actions -- adding items, running reports, counting stock, sending to terminal. Voice flow uses OpenAI's Realtime API via WebRTC (browser PWA), syncing operations live to Square.

## Monorepo Structure

pnpm workspace (Node 22, TypeScript 5.9, pnpm 9.15.9) with two package directories:

- **`lib/`** -- shared libraries consumed by artifacts
  - `db` -- Drizzle ORM + PostgreSQL (+ pgvector) schema. Run `pnpm push` inside this package to migrate.
  - `voicelab-core` -- Core business logic: pricing plans, skills, noise modes, confirmation policies, voice pipeline types
  - `voicelab-client` -- Shared client-side logic: voice-termination, voice-prefs, order reducer (used by PWA and landing)
  - `api-zod` -- Zod validation schemas generated by Orval from OpenAPI spec

- **`artifacts/`** -- deployable applications
  - `api-server` -- Express 5 backend: REST API, WebSocket relay, serves both frontends as static files in production. Imports `@workspace/db`, `@workspace/api-zod`, `@workspace/voicelab-core`.
  - `voice-agent-pwa` -- React + Vite PWA for browser-based voice ordering via WebRTC. Imports `@workspace/voicelab-client`.
  - `voycelab-landing` -- React + Vite + Tailwind + shadcn/ui dashboard (auth, venue management, account settings). Uses Wouter for routing. Imports `@workspace/voicelab-client`.

## Commands

```bash
# Install dependencies
pnpm install

# Full build (typecheck + all artifacts)
pnpm build

# Typecheck only
pnpm typecheck

# Railway-specific build (sequential: typecheck -> build landing -> build PWA -> build API)
pnpm build:railway

# Start production server
pnpm start

# Local dev (Windows) -- starts API (8080), landing (5173), PWA (8081)
start-all.bat

# Individual dev servers
cd artifacts/api-server && pnpm dev          # API on :8080
cd artifacts/voycelab-landing && pnpm dev    # Landing on :5173
cd artifacts/voice-agent-pwa && pnpm dev     # PWA on :8081

# Database migration
cd lib/db && pnpm push

# One-time migration scripts
cd artifacts/api-server && npx tsx scripts/encrypt-venue-tokens.ts
cd artifacts/api-server && npx tsx scripts/migrate-venues-to-orgs.ts
cd artifacts/api-server && npx tsx scripts/migrate-legacy-plans.ts
cd artifacts/api-server && npx tsx scripts/migrate-embeddings-to-pgvector.ts
cd artifacts/api-server && npx tsx scripts/enable-pgvector.ts
```

## Architecture

**Voice ordering flow**: PWA requests an ephemeral OpenAI token from `POST /api/realtime/session` (which also returns server-built instructions) -> establishes WebRTC RTCPeerConnection directly to OpenAI Realtime API -> user speaks -> OpenAI transcribes and triggers tool calls -> client POSTs tool execution to `POST /api/realtime/tools` -> API server executes Square operations via `square-helpers.ts` -> order synced to Square POS.

**Server is authoritative** for persona, confirmation policy, voice pipeline selection, noise-mode VAD settings, and tool gating. Clients are dumb terminals -- they never build their own instructions.

**Single-server deployment**: The API server serves the landing app at `/`, the PWA at `/agent/`, API routes at `/api/*`, and a WebSocket relay for server-relayed voice providers. All deployed as one Railway service.

**Auth**: JWT-based. `requireAuth` and `requirePlan` middleware gate API routes. `req.organization` is attached alongside `req.user`. Clerk Billing (B2B, organization-level) manages subscriptions (plan/trial gating).

**Square integration**: OAuth flow at `/api/square/oauth/*` stores encrypted tokens per-venue. Credentials are AES-256-GCM encrypted via `secrets.ts`. The credential cache decrypts on read.

## Skills System

Tools are organized into composable **skills** (`api-server/src/skills/`), each bundling related tools with system prompt segments and subscription tier gating:

| Skill | Tier | Tools |
|-------|------|-------|
| `pos` | core | add_item, remove_item, get_order, clear_order, submit_order, send_to_terminal, search_menu |
| `orders-reporting` | core | list_orders, sales_report, list_open_orders, get_order_details, hourly_sales, item_performance, daily_summary, list_locations |
| `inventory` | standard | check_inventory, check_all_inventory, adjust_inventory, set_inventory, transfer_inventory, get_inventory_changes, low_stock_report, get_item_details, batch_adjust_inventory, inventory_summary |
| `catalog-management` | standard | create_item, update_item, delete_item, list_categories, create_category, list_modifiers, apply_discount |
| `customers-payments` | standard | search_customer, create_customer, get_customer, update_customer, list_payments, refund_payment, cancel_payment |
| `team-labor` | premium | list_team, current_shifts, clock_in, clock_out |

Sessions load only the skills matching the user's subscription plan (`getSkillsForSession(plan)`). System prompt is composed from skill instruction segments.

## Workflow Engine

Multi-step workflows (`api-server/src/workflows/`) orchestrate tool sequences and register as callable tools:

- `end_of_day_close` -- daily_summary -> list_open_orders -> low_stock_report
- `opening_checklist` -- inventory_summary -> low_stock_report(10) -> current_shifts -> list_locations
- `stock_take` -- check_all_inventory -> inventory_summary

Workflows are exposed via `POST /v1/workflows/:slug/run` (SSE streaming) and as one-tap buttons in both the dashboard and PWA.

## Tool Middleware

All tool executors are wrapped with middleware (`api-server/src/tools/middleware.ts`):
- **confirmationMiddleware** -- blocks high/medium risk tools unless `confirmed: true` is passed; returns `REQUIRES_CONFIRMATION` result instead
- **errorMiddleware** -- catches thrown errors, returns normalized ToolResult
- **timingMiddleware** -- logs execution duration, warns on >2s
- **loggingMiddleware** -- logs tool invocations with sanitized args
- **auditMiddleware** -- inserts one `tool_calls` row per execution (fire-and-forget)

Custom middleware follows the `ToolMiddleware` signature: `(toolName, args, ctx, next) => Promise<ToolResult>`.

## Confirmation Gates

The `confirmed: true` flow:
1. Client calls `POST /api/realtime/tools` with a tool_name
2. `confirmationMiddleware` checks `requiresConfirmation(toolName, riskLevel, noiseMode)` from voicelab-core
3. If confirmation required and `confirmed` not true, returns `{ status: "REQUIRES_CONFIRMATION", confirmation: { tool_name, risk_level, prompt } }`
4. PWA shows a sticky banner with Confirm/Cancel buttons
5. Confirm replays the call with `confirmed: true`; Cancel sends a decline output to the model

Risk levels are defined in `lib/voicelab-core/src/confirmation/types.ts` (`TOOL_RISK_DEFAULTS`).

## Noise Modes

Each agent profile has a `noiseMode` (quiet_room, restaurant, bar, nightclub, event_venue, manual_push_to_talk). This maps to OpenAI Realtime turn_detection settings via `buildRealtimeSessionConfig`:
- quiet_room: semantic_vad, eagerness high, interrupt enabled
- restaurant: semantic_vad, eagerness auto, interrupt enabled
- bar: server_vad, threshold 0.55, silence 700ms
- nightclub: server_vad, threshold 0.75, silence 1200ms, no interrupt, push-to-talk
- event_venue: server_vad, threshold 0.7, push-to-talk preferred
- manual_push_to_talk: turn_detection null

Noise behaviors are defined in `lib/voicelab-core/src/noise/behaviors.ts`.

## Telemetry and Metering

- **tool_calls table**: every tool execution writes one row (via auditMiddleware, fire-and-forget)
- **usage_events table**: voice minutes tracked via PWA heartbeat (60s interval) + session end POST
- **Overage enforcement**: `requirePlan` checks `usage_events.minutes` against `plan.includedVoiceMinutes * 1.5`; returns 402 past the cap
- **Usage dashboard**: `GET /v1/usage/current` returns voice minutes, top tools, recent errors; rendered in `/settings` UsageCard

## Pricing

| Plan | Voice Minutes | Overage | Pipelines |
|------|---------------|---------|-----------|
| Trial (14d) | 100 | none | OpenAI WebRTC + fallbacks |
| Starter ($79/mo) | 250 | $0.20/min | OpenAI WebRTC + WS |
| Professional ($199/mo) | 1,000 | $0.17/min | + Gemini |
| Premium ($499/mo) | 4,000 | $0.14/min | All engines |
| Enterprise | Custom | Custom | All engines |

All plans have a 1.5x overage hard cap. Seven voice pipeline providers (LiveKit, Pipecat, Hume, 4 modular) are marked "request_only" and throw `pipeline_not_provisioned` if selected.

## Performance Layer

- **SquareClient** (`lib/square-client.ts`) -- fetch wrapper with exponential backoff retry (3 attempts), circuit breaker per venue (opens after 5 failures in 60s)
- **Credential Cache** (`lib/credential-cache.ts`) -- 5-min TTL in-memory cache for venue Square credentials (decrypted on read), avoids DB hit on every tool call
- **Catalog Cache** (`lib/catalog-cache.ts`) -- 5-min TTL per-venue catalog cache, avoids re-fetching from Square API
- **Session Store** (`lib/session-store.ts`) -- in-memory + DB write-through for voice session state, survives server restarts
- **Pino Logging** (`lib/logger.ts`) -- structured JSON logging via pino + pino-http; LOG_LEVEL env var

## Landing Page Design System

The marketing site (`voycelab-landing`) uses a warm, hospitality-focused light theme.

**Visual identity**: Cream paper backgrounds, deep navy ink, coral accent, painterly watercolor washes. Serif display headlines (Fraunces), sans-serif body (Inter).

**Key tokens** (defined in `lib/tokens.ts` and `index.css`):
- Background: `#FBF7F1` (cream) / Ink: `#0E1B2C` (navy) / Accent: `#FF6B47` (coral)

**Customer-facing language**: Say "assistant" not "agent". Say "commands" not "tools". Say "connected systems" not "APIs". Never expose technical internals in user-facing UI.

**Landing demo**: Mock-bar demo backed by the mock connector (`/api/realtime/demo-bar-session`). Users talk to "Bev" and see a live order ticket update in real time. Same code path as production Square sync.

## Key Conventions

- Workspace packages use `"customConditions": ["workspace"]` in tsconfig for cross-package imports
- API server bundles to a single CJS file via esbuild (`artifacts/api-server/build.ts`)
- PWA builds with `BASE_PATH=/agent/` for production sub-path serving
- Database schema lives in `lib/db/src/schema/` using Drizzle table definitions
- Knowledge chunks use pgvector `vector(1536)` with HNSW cosine index for embedding search
- Route handlers are in `artifacts/api-server/src/routes/` (auth, square, venues, realtime, subscriptions)
- Tool domain modules are in `api-server/src/tools/` -- each exports `definitions` and `executors`
- Skill modules are in `api-server/src/skills/` -- each wraps tool modules with instructions and tier

## Environment Variables

Required: `DATABASE_URL`, `JWT_SECRET`, `SECRETS_ENCRYPTION_KEY` (dedicated 32+ character secret for AES-256-GCM key derivation), `OPENAI_API_KEY`, `GOOGLE_GEMINI_API_KEY`, `SQUARE_APPLICATION_ID`, `SQUARE_APPLICATION_SECRET`, `PUBLIC_BASE_URL`, `PORT`

Provider API keys are resolved through `artifacts/api-server/src/lib/api-keys.ts`. Use canonical names for new deployments: `OPENAI_API_KEY` and `GOOGLE_GEMINI_API_KEY`. Legacy aliases (`AI_INTEGRATIONS_OPENAI_API_KEY`, `GOOGLE_API_KEY`) are accepted only for compatibility.

Optional: `VITE_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`, `CLERK_PLAN_PRO_ID`, `CLERK_PLAN_BUSINESS_ID`, `SESSION_SECRET`, `LOG_LEVEL`
