# Server
NODE_ENV=development
PORT=8080
PUBLIC_BASE_URL=http://localhost:8080
DATABASE_URL=postgres://user:password@localhost:5432/voycelab
JWT_SECRET=replace-with-a-random-32-plus-character-session-secret
SECRETS_ENCRYPTION_KEY=replace-with-a-dedicated-random-32-plus-character-secret

# Provider API keys (canonical names)
OPENAI_API_KEY=
GOOGLE_GEMINI_API_KEY=

# OpenAI GPT-Live voice defaults
# Voice is gpt-live-1. Task reasoning is delegated to this Responses model.
OPENAI_LIVE_BACKEND_MODEL=gpt-5.6-terra

# Autonomous business control plane
# Safe default: the company brain and risky side effects are OFF until enabled.
AUTONOMY_ENABLED=false
AUTONOMY_MODEL=gpt-5.6-terra
AUTONOMY_FAST_MODEL=gpt-5.6-luna
AUTONOMY_ENGINEERING_TRIAGE_MODEL=gpt-5.6-terra
AUTONOMY_ENGINEERING_MODEL=gpt-5.6-sol

# Operator identity whose configured Gmail/Resend/SMTP account handles lifecycle,
# sales, support and outbound messages.
AUTONOMY_OPERATOR_USER_ID=
AUTONOMY_OPERATOR_ORG_ID=
AUTONOMY_DEMO_URL=
# Comma/semicolon/space-separated accounts that must never receive autonomous
# lifecycle mail or be treated as support customers. Use for founders, staff,
# seed/demo users and known test accounts.
AUTONOMY_INTERNAL_EMAILS=

# Independent authority switches. Keep outbound OFF until contact verification
# and sender reputation are healthy; hard-bounce reconciliation runs regardless.
AUTONOMY_ENABLE_OUTBOUND=false
AUTONOMY_ENABLE_SALES=true
AUTONOMY_ENABLE_CUSTOMER_SUCCESS=true
AUTONOMY_ENABLE_SUPPORT=true
AUTONOMY_ENABLE_CODE_WRITES=false
AUTONOMY_AUTO_MERGE_CODE=false
AUTONOMY_ALLOW_MERGE_WITHOUT_CHECKS=false

# Runtime GitHub credential for the deployed product-engineering worker. Use a
# narrowly scoped GitHub App/PAT with contents + pull-request rights only here.
GITHUB_AUTONOMY_TOKEN=
GITHUB_AUTONOMY_REPO=Smarticus81/squareagent
GITHUB_AUTONOMY_BASE_BRANCH=master

# Business authority / spend / communication ceilings. These conservative email
# defaults protect sender reputation; every prospect must also pass official-site
# exact-email verification plus an MX check before a send is allowed.
AUTONOMY_MAX_OUTBOUND_PER_DAY=20
AUTONOMY_MAX_OUTBOUND_PER_DOMAIN_DAY=1
AUTONOMY_MAX_AD_SPEND_CENTS_DAY=20000
AUTONOMY_MAX_AGENT_COMPUTE_CENTS_DAY=5000
AUTONOMY_MAX_AUTO_CREDIT_CENTS=2500
AUTONOMY_MAX_PRICE_TEST_PERCENT=15
AUTONOMY_MAX_CAMPAIGN_INCREASE_PERCENT=20
AUTONOMY_CODE_CANARY_PERCENT=5
# Kill a paid-conversion campaign after this many confirmed deliveries if it has
# produced zero attributed paid customers, then force a materially new angle.
AUTONOMY_ZERO_CONVERSION_ROTATE_AFTER_SENDS=12

# Finance / unit-economics inputs. These are deliberately deployment-configured
# rather than hard-coded because provider and infrastructure costs change.
# Finance will not claim complete cost coverage until all three are supplied.
AUTONOMY_BLENDED_VOICE_COST_CENTS_PER_MINUTE=
AUTONOMY_FIXED_INFRA_COST_CENTS_MONTH=
AUTONOMY_AGENT_COMPUTE_COST_CENTS_MONTH=
AUTONOMY_MIN_GROSS_MARGIN_PERCENT=60
AUTONOMY_MAX_CAC_PAYBACK_MONTHS=12

# Experiment safety / statistical thresholds
AUTONOMY_EXPERIMENT_MIN_SAMPLE_PER_VARIANT=50
AUTONOMY_EXPERIMENT_MAX_SAMPLE_PER_VARIANT=250
AUTONOMY_GUARDRAIL_MIN_SAMPLE_PER_VARIANT=20

# Revenue-first scheduler cadences (minutes). Strategy checks hourly; signup and
# activation interventions run every 15 minutes. Deliverability/send caps remain
# independent guardrails.
AUTONOMY_STRATEGY_INTERVAL_MINUTES=60
AUTONOMY_INBOX_INTERVAL_MINUTES=10
AUTONOMY_ACTIVATION_INTERVAL_MINUTES=15
AUTONOMY_PROMOTION_INTERVAL_MINUTES=10
# Override when the operator inbox contains unrelated mail. Support always
# excludes mail from the operator's own Gmail identity.
AUTONOMY_SALES_GMAIL_QUERY="in:inbox is:unread newer_than:14d"
AUTONOMY_SUPPORT_GMAIL_QUERY="in:inbox is:unread newer_than:7d -from:me"

# Platform password recovery email
AUTH_EMAIL_FROM="VoyceLab <no-reply@yourdomain.com>"
AUTH_RESEND_API_KEY=
# Or SMTP:
AUTH_SMTP_HOST=
AUTH_SMTP_PORT=587
AUTH_SMTP_USER=
AUTH_SMTP_PASS=
AUTH_SMTP_SECURE=false

# Square OAuth
SQUARE_APPLICATION_ID=
SQUARE_APPLICATION_SECRET=

# Clerk Billing
VITE_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=
CLERK_PLAN_PRO_ID=
CLERK_PLAN_BUSINESS_ID=
CLERK_CHECKOUT_PRO_MONTHLY_URL=
CLERK_CHECKOUT_PRO_YEARLY_URL=
CLERK_CHECKOUT_BUSINESS_MONTHLY_URL=
CLERK_CHECKOUT_BUSINESS_YEARLY_URL=
CLERK_BILLING_PORTAL_URL=

# Optional
SESSION_SECRET=
LOG_LEVEL=debug
VITE_DEMO_BOOKING_URL=
VITE_SALES_EMAIL=hello@voycelab.ai
