import { pool } from "@workspace/db";
import { DEFAULT_AUTONOMY_BUDGET, outboundEnabled } from "./constitution";
import { structuredModel } from "./openai";
import { recordAutonomousAction, markActionExecuted, markActionFailed, recordBusinessEvent } from "./ledger";
import { assignOutboundCampaign } from "./marketing";
import { executors as emailExecutors } from "../tools/general/email";
interface ResearchLead{companyName:string;website:string|null;contactName:string|null;contactEmail:string|null;segment:"wedding_venue"|"event_venue"|"bar_restaurant"|"hospitality_group"|"other";fitScore:number;reason:string;evidence:string[];}
const LEAD_SCHEMA={type:"object",additionalProperties:false,required:["leads","marketObservation"],properties:{marketObservation:{type:"string"},leads:{type:"array",maxItems:10,items:{type:"object",additionalProperties:false,required:["companyName","website","contactName","contactEmail","segment","fitScore","reason","evidence"],properties:{companyName:{type:"string"},website:{type:["string","null"]},contactName:{type:["string","null"]},contactEmail:{type:["string","null"]},segment:{type:"string",enum:["wedding_venue","event_venue","bar_restaurant","hospitality_group","other"]},fitScore:{type:"number",minimum:0,maximum:100},reason:{type:"string"},evidence:{type:"array",items:{type:"string"},maxItems:8}}}}}} as const;
const EMAIL_SCHEMA={type:"object",additionalProperties:false,required:["subject","body"],properties:{subject:{type:"string"},body:{type:"string"}}} as const;
function validEmail(v:string|null):v is string{return Boolean(v&&/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(v));}
async function resolveOperatorUserId():Promise<number>{if(!pool)throw new Error("Database is required for operator resolution");const raw=process.env.AUTONOMY_OPERATOR_USER_ID?.trim();if(!raw)throw new Error("AUTONOMY_OPERATOR_USER_ID is required for autonomous outbound");const numeric=Number(raw);if(Number.isInteger(numeric)&&numeric>0)return numeric;const r=await pool.query<{id:number}>(`SELECT id FROM users WHERE clerk_user_id=$1::text OR lower(email)=lower($1::text) LIMIT 1`,[raw]);const id=Number(r.rows[0]?.id);if(!Number.isInteger(id)||id<=0)throw new Error("AUTONOMY_OPERATOR_USER_ID must be a local user id, Clerk user id, or account email linked to VoyceLab");return id;}
export async function researchProspects(runId?:string):Promise<{discovered:number;marketObservation:string}>{if(!pool)throw new Error("Database is required for autonomous growth");const research=await structuredModel<{leads:ResearchLead[];marketObservation:string}>(["You are VoyceLab's market intelligence worker. VoyceLab is a voice-powered operations assistant for hospitality venues, with Square-connected POS actions, inventory, reporting, customers/payments and team/labor workflows.","Use current public web information to find real US businesses that appear to be strong prospects, prioritizing event venues, wedding venues, multi-location hospitality groups, and operational environments where staff repeatedly leave the floor to interact with POS/back-office systems.","Only include contact names/emails when explicitly supported by public evidence. Never infer or fabricate an email address.","Do not collect sensitive personal information. Business contact information only.","Prefer high-intent evidence such as Square usage, event volume, multiple venues, active hiring, operational complexity, or public descriptions of their venue operations.","Fit score should reflect likely product value and realistic salesability, not business prestige."].join("\n"),{target:"qualified VoyceLab customers",geography:"United States",maxLeads:10},{schemaName:"voycelab_growth_research",schema:LEAD_SCHEMA as unknown as Record<string,unknown>,useWebSearch:true,reasoningEffort:"medium",maxOutputTokens:4200});let discovered=0;for(const lead of research.leads){if(!lead.companyName||lead.fitScore<55)continue;const existing=await pool.query(`SELECT id FROM prospect_leads WHERE (website IS NOT NULL AND website=$1::text) OR (contact_email IS NOT NULL AND contact_email=$2::text) LIMIT 1`,[lead.website,lead.contactEmail]);if(existing.rowCount)continue;await pool.query(`INSERT INTO prospect_leads (company_name,website,contact_name,contact_email,segment,fit_score,evidence,profile,next_contact_at) VALUES ($1::text,$2::text,$3::text,$4::text,$5::text,$6::integer,$7::jsonb,$8::jsonb,CASE WHEN $4::text IS NULL THEN NULL ELSE now() END)`,[lead.companyName,lead.website,lead.contactName,validEmail(lead.contactEmail)?lead.contactEmail:null,lead.segment,Math.round(lead.fitScore),JSON.stringify(lead.evidence),JSON.stringify({reason:lead.reason})]);discovered++;}await recordBusinessEvent({eventType:"growth_research_completed",actorType:"agent",actorId:"growth-research",properties:{discovered,marketObservation:research.marketObservation,runId}});return{discovered,marketObservation:research.marketObservation};}
async function sendsToday(){if(!pool)return 0;const r=await pool.query<{count:number}>(`SELECT COUNT(*)::int AS count FROM business_events WHERE event_type='outbound_sent' AND occurred_at>=date_trunc('day',now())`);return Number(r.rows[0]?.count??0);}
async function sendsToDomainToday(domain:string){if(!pool)return 0;const r=await pool.query<{count:number}>(`SELECT COUNT(*)::int AS count FROM business_events WHERE event_type='outbound_sent' AND occurred_at>=date_trunc('day',now()) AND properties->>'domain'=$1::text`,[domain]);return Number(r.rows[0]?.count??0);}
export async function runOutboundBatch(runId?:string,maxBatch=12):Promise<{sent:number;skipped:number}>{if(!pool)throw new Error("Database is required for autonomous outbound");if(!outboundEnabled())return{sent:0,skipped:0};const operatorUserId=await resolveOperatorUserId(),operatorOrgId=process.env.AUTONOMY_OPERATOR_ORG_ID?.trim()||null;const remaining=Math.max(0,DEFAULT_AUTONOMY_BUDGET.maxOutboundPerDay-await sendsToday()),limit=Math.max(0,Math.min(maxBatch,remaining));if(!limit)return{sent:0,skipped:0};const leads=await pool.query(`SELECT * FROM prospect_leads WHERE stage IN ('new','nurture') AND contact_email IS NOT NULL AND (next_contact_at IS NULL OR next_contact_at<=now()) ORDER BY fit_score DESC,created_at ASC LIMIT $1::integer`,[limit*3]);let sent=0,skipped=0;for(const lead of leads.rows){if(sent>=limit)break;const email=String(lead.contact_email??"").trim().toLowerCase();if(!validEmail(email)||lead.stage==="do_not_contact"){skipped++;continue;}const domain=email.split("@")[1];if(!domain||await sendsToDomainToday(domain)>=DEFAULT_AUTONOMY_BUDGET.maxOutboundPerDomainPerDay){skipped++;continue;}const campaign=await assignOutboundCampaign(String(lead.id));const copy=await structuredModel<{subject:string;body:string}>(["Write a short, respectful B2B first-touch email for VoyceLab.","Use ONLY supplied public evidence for personalization; never fabricate.","VoyceLab lets hospitality teams operate connected systems such as Square through voice commands for POS, inventory, reporting and venue operations.","Faithfully express the supplied campaign strategy while obeying its proofConstraint.","Plain text, under 120 words, one CTA, no fake urgency. End with: If this isn't relevant, just say so and I won't follow up."].join("\n"),{companyName:lead.company_name,contactName:lead.contact_name,segment:lead.segment,evidence:lead.evidence,fitScore:lead.fit_score,campaign:campaign?{slug:campaign.slug,variantId:campaign.variantId,strategy:campaign.payload}:null},{schemaName:"voycelab_outbound_email",schema:EMAIL_SCHEMA as unknown as Record<string,unknown>,reasoningEffort:"low",maxOutputTokens:700});const action=await recordAutonomousAction({runId,agent:"growth-outbound",actionType:"outreach.email",riskLevel:"medium",input:{leadId:lead.id,to:email,subject:copy.subject,campaign:campaign?.slug??null,variant:campaign?.variantId??null},expectedImpact:{goal:"qualified_reply_or_trial",primaryMetric:"outbound_positive_reply"}});if(action.authority==="founder"||action.authority==="forbidden"){skipped++;continue;}try{const executor=emailExecutors.send_email;if(!executor)throw new Error("send_email executor is unavailable");const result=await executor({to:email,subject:copy.subject,body:copy.body},{userId:operatorUserId,organizationId:operatorOrgId} as any);if(/failed|error|missing|limit|rejected/i.test(result.result))throw new Error(result.result);await pool.query(`UPDATE prospect_leads SET stage='contacted',last_contacted_at=now(),next_contact_at=now()+interval '5 days',updated_at=now() WHERE id=$1::uuid`,[lead.id]);await recordBusinessEvent({eventType:"outbound_sent",actorType:"agent",actorId:"growth-outbound",source:"autonomous_outbound",campaign:campaign?.slug??null,experimentId:campaign?.experimentId??null,variant:campaign?.variantId??null,properties:{leadId:lead.id,domain,segment:lead.segment,fitScore:lead.fit_score},dedupeKey:`outbound:${lead.id}:${new Date().toISOString().slice(0,10)}`});await markActionExecuted(action.id,{providerResult:result.result,campaign:campaign?.slug??null,variant:campaign?.variantId??null});sent++;}catch(error){await markActionFailed(action.id,{error:error instanceof Error?error.message:String(error)});skipped++;}}return{sent,skipped};}
export async function optOutLead(leadId:string,reason="recipient_opt_out"):Promise<void>{if(!pool)return;await pool.query(`UPDATE prospect_leads SET stage='do_not_contact',next_contact_at=NULL,updated_at=now() WHERE id=$1::uuid`,[leadId]);await recordBusinessEvent({eventType:"outreach_opt_out",actorType:"customer",properties:{leadId,reason},dedupeKey:`optout:${leadId}`});}
